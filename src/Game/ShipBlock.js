import React from 'react';

export const ShipBlock = ({ shipName }) => {
  // get ship length, e.g. 5 for carrier
  // onHover
  // onClick
  // get the ID of the square that was pressed on
  // find which item it is in the layout array
  // replace
};
